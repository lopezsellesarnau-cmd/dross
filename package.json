{
  "name": "dross",
  "version": "0.1.0",
  "description": "Scans a repo before deploy and reports on dead code, contradictions, and contract drift between frontend and backend.",
  "type": "module",
  "bin": {
    "dross": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "eval": "tsx eval/run.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
