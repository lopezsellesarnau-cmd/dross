node_modules/
dist/
*.log
.dross/
.DS_Store

# Release/build artifacts (regenerable via scripts/package-dmg.sh + rebuild.sh)
dist-release/
app/.build/
*.dmg

# License signing secret — NEVER commit. Back this up outside the repo.
license-private.pem
*-private.pem
